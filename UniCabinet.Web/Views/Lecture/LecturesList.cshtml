@model List<UniCabinet.Web.ViewModel.Lecture.LectureViewModel>
@{
    ViewBag.Title = "Список лекций";
}

<h2>Список лекций по дисциплине @ViewBag.Discipline</h2>

<button class="btn btn-primary" onclick="openModal('/Lecture/LectureAddModal', 'lectureAddModal', @ViewBag.DisciplineDetaildId)">Добавить лекцию</button>

<table class="table">
    <thead>
        <tr>
            <th>Номер лекции</th>
            <th>Дата</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var lecture in Model)
        {
            <tr>
                <td>@lecture.Number</td>
                <td>@lecture.Date.ToShortDateString()</td>
                <td>
                    <button class="btn btn-sm btn-warning" onclick="openModal('/Lecture/LectureEditModal', 'lectureEditModal', @lecture.Id)">Редактировать</button>
                </td>
            </tr>
        }
    </tbody>
</table>

<div id="modalContainer"></div>


